this reads text from an input file called "input.txt", processes the text to count the occurrences of each word in a case-insensitive manner, and then generates a json-like output in the "output.json" file
